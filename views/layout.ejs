<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title><%= typeof title !== 'undefined' ? title : 'Evalio | Plateforme d\'examens en ligne' %></title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;800&display=swap" rel="stylesheet"/>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    body { font-family: 'Poppins', sans-serif; }
    .hero-gradient { background: linear-gradient(to right, #d2f1e9, #f5f6f8); }
    .floating-img {
      animation: float 6s ease-in-out infinite;
    }
    @keyframes float {
      0%, 100% { transform: translateY(0); }
      50% { transform: translateY(-10px); }
    }
  </style>
  
  <!-- Extra Styles (if any) -->
  <%- typeof extraStyles !== 'undefined' ? extraStyles : '' %>
</head>
<body class="bg-white text-gray-800">

  <!-- Navbar -->
  <%- include('partials/navbar') %>

  <!-- Success Message -->
  <% if (typeof success !== 'undefined' && success.length > 0) { %>
    <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded-lg mx-8 my-4">
      <%= success %>
    </div>
  <% } %>

  <!-- Error Message -->
  <% if (typeof error !== 'undefined' && error.length > 0) { %>
    <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded-lg mx-8 my-4">
      <%= error %>
    </div>
  <% } %>

  <!-- Main Content (Body) -->
  <%- body %>

  <!-- Footer -->
  <footer class="py-10 text-center bg-white text-gray-400 text-sm border-t">
    Copyright © 2025 – Evalio
  </footer>

  <!-- Custom JS -->
  <script src="/static/js/main.js"></script>
  
  <!-- Extra Scripts (if any) -->
  <%- typeof extraScripts !== 'undefined' ? extraScripts : '' %>

</body>
</html>

